# دليل التثبيت - Water Flow 💧

## المتطلبات

- **Windows 7 فما فوق**
- **.NET Framework 4.7.2 أو أحدث**
- **Visual Studio 2019 أو 2022** (للتطوير)
- **500 MB من مساحة التخزين**

## خطوات التثبيت

### 1. استنساخ المستودع
```bash
git clone https://github.com/abohoor221296-pixel/Water-Flow.git
cd Water-Flow
```

### 2. تثبيت الحزم (NuGet Packages)
افتح Package Manager Console في Visual Studio وقم بتشغيل:
```bash
Update-Package -Reinstall
```

### الحزم المطلوبة:
- `EntityFramework` (v6.0.0 أو أحدث)
- `System.Data.SQLite` (v1.0.118 أو أحدث)
- `EPPlus` (v5.0 أو أحدث)
- `iTextSharp` (v5.5.13 أو أحدث)

### 3. إنشاء قاعدة البيانات
قم بتشغيل الـ Package Manager Console وأدخل:
```bash
Enable-Migrations
Add-Migration InitialCreate
Update-Database
```

### 4. تشغيل التطبيق
- اضغط `F5` أو **Debug → Start Debugging**
- أو انقر على زر التشغيل الأخضر

## الإعدادات الأولية

### إضافة ملف Excel
1. ضع ملف `WaterFlow.xlsx` في نفس مجلد التطبيق
2. انتقل إلى **الإعدادات** في التطبيق
3. حدد مسار ملف Excel
4. اختر **مزامنة البيانات**

### إنشاء التصنيفات الأولية
1. اذهب إلى **الأصناف والتصنيفات**
2. انقر على **إضافة تصنيف جديد**
3. أدخل اسم التصنيف والوصف
4. انقر **حفظ**

## استكشاف الأخطاء

### المشكلة: قاعدة البيانات لا تنشأ
**الحل:**
- تأكد من وجود .NET Framework 4.7.2
- حاول حذف الملف `WaterFlow.db` وأعد تشغيل التطبيق
- تحقق من أن المجلد قابل للكتابة

### المشكلة: خطأ في ربط Excel
**الحل:**
- تأكد من أن ملف Excel مغلق أثناء المزامنة
- جرب نسخة Excel مختلفة (xlsx بدلاً من xls)
- تحقق من أن الأعمدة في Excel تطابق النموذج

### المشكلة: الواجهة لا تظهر بشكل صحيح
**الحل:**
- تحديث .NET Framework
- تعديل الإعدادات → عرض → دقة الشاشة

## الدعم الفني

للإبلاغ عن مشاكل أو اقتراح مميزات:
- أنشئ issue على GitHub
- أرسل بريد إلى: [البريد الإلكتروني]

---

**نسخة:** 1.0.0  
**آخر تحديث:** 2026
