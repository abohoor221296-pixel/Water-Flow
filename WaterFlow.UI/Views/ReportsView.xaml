<Page x:Class="WaterFlow.UI.Views.ReportsView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="التقارير" Loaded="Page_Loaded"
      Background="White">
    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="التقارير الديناميكية" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" Foreground="#2C3E50"/>

        <!-- خيارات التقرير -->
        <Grid Grid.Row="1" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="نوع التقرير:" VerticalAlignment="Center" Margin="0,0,10,0" Foreground="#2C3E50" FontWeight="Bold"/>
            <ComboBox Grid.Column="1" Margin="0,0,10,0" Padding="10" Background="#ecf0f1" Opacity="0.9">
                <ComboBoxItem>يومي</ComboBoxItem>
                <ComboBoxItem>أسبوعي</ComboBoxItem>
                <ComboBoxItem>شهري</ComboBoxItem>
                <ComboBoxItem>مخصص</ComboBoxItem>
            </ComboBox>

            <Border Grid.Column="2" Background="#3498db" CornerRadius="3" Margin="0,0,10,0">
                <Button Content="📊 إنشاء" Click="BtnGenerateReport_Click"
                        Background="Transparent" Foreground="White" FontWeight="Bold" BorderThickness="0"/>
            </Border>
            <Border Grid.Column="3" Background="#e74c3c" CornerRadius="3" Margin="0,0,10,0">
                <Button Content="📄 PDF" Click="BtnExportPDF_Click"
                        Background="Transparent" Foreground="White" FontWeight="Bold" BorderThickness="0"/>
            </Border>
            <Border Grid.Column="4" Background="#27ae60" CornerRadius="3">
                <Button Content="📈 Excel" Click="BtnExportExcel_Click"
                        Background="Transparent" Foreground="White" FontWeight="Bold" BorderThickness="0"/>
            </Border>
        </Grid>

        <!-- عرض التقرير -->
        <DataGrid Grid.Row="2" x:Name="ReportsGrid" AutoGenerateColumns="True"
                  Background="#ecf0f1" GridLinesVisibility="Horizontal"/>
    </Grid>
</Page>